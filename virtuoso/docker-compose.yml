version: '3.4'
services:
  virtuoso:
     build: .
     image: tenforce/virtuoso:1.3.1-virtuoso7.2.2
     environment:
        - SPARQL_UPDATE=false
        - DBA_PASSWORD
        - DEFAULT_GRAPH=http://glygen.org/glygen
     volumes:
        - ${HOME}/${SITE_CODE:-glygen-array}/virtuoso:/data
     ports:
       - "8890:8890"
       - "1111:1111"
networks:
  default:
    external:
      name: ${GLYGEN_ARRAY_NETWORK:-glygen-array-network}
